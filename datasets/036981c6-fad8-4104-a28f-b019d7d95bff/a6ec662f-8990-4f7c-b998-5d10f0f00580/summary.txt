The paper argues that ISO C, through its evolution and emphasis on compiler optimizations, has become poorly suited for operating system development. Key issues include the proliferation of undefined behavior, strict aliasing rules, and the loss of semantic clarity, which force OS developers to rely on non-standard dialects and compiler-specific workarounds. The author contends that these changes undermine C's original purpose as a 'high-level assembler' and introduce reliability and portability